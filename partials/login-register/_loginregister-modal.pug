.login.modal.fade(tabindex='-1', role='dialog', aria-hidden='true')
	.modal-dialog.modal-dialog-centered(role='document')
		.modal-content
			.modal-header
				include _avatar
				button.close(type='button', data-dismiss='modal', aria-label='Close')
					span(aria-hidden='true') &times;
				
			.modal-body
				include _enter
	.modal-dialog.modal-dialog-centered(role='document')
		.modal-content
			.modal-header
				include _avatar
				button.close(type='button', data-dismiss='modal', aria-label='Close')
					span(aria-hidden='true') &times;
				
			.modal-body
				include _verification
	.modal-dialog.modal-dialog-centered(role='document')
		.modal-content
			.modal-header
				include _avatar
				button.close(type='button', data-dismiss='modal', aria-label='Close')
					span(aria-hidden='true') &times;
				
			.modal-body
				include _setpassword
	.modal-dialog.modal-dialog-centered(role='document')
		.modal-content
			.modal-header
				include _avatar
				button.close(type='button', data-dismiss='modal', aria-label='Close')
					span(aria-hidden='true') &times;
				
			.modal-body
				include _getpassword
